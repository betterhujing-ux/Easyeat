# AGENTS.md - 智能体协作指导与设计原则基石

本项目已被赋予严谨的**《人机界面指南》（Human Interface Guidelines）八大核心设计原则**作为一切功能规划、界面重构与代码产出的根本指导标准。所有参与本项目的 AI 编码智能体必须严格遵循以下原则与工程约束：

---

## 1. 核心设计原则 (HIG 8 Foundational Principles)

详细原则定义见 [`DESIGN_PRINCIPLES.md`](./DESIGN_PRINCIPLES.md)，无障碍落地规范见 [`ACCESSIBILITY.md`](./ACCESSIBILITY.md)。

1. **目标感 (Purpose)**：创造有意义的产品。保持专注，优先核心体验，拒绝功能堆砌与不相关的外部插件。
2. **能动性 (Agency)**：让用户随心所欲。不妨碍工作，支持自由探索，提供清晰的容错机制（如删除二次确认、可随时取消与重填）。
3. **责任感 (Accountability)**：以用户利益为核心。尊重用户隐私，纯本地安全持久化，文案说明诚恳透明，不夸大功效。
4. **熟悉感 (Familiarity)**：基于生活常识与认知习惯。符合自然一日饮食与烹饪逻辑，交互反馈即时明确。
5. **灵活 (Flexibility)**：为所有人设计。确保移动端至少 44px 点击热区，遵循 WCAG AA 级对比度，支持键盘与屏幕阅读器无障碍。
6. **简洁 (Simplicity)**：清晰直接。纯白背景卡片、0.5px 极细浅灰边框 (`#E0E0E0`)、彻底移除阴影装饰与花哨渐变，建立扁平清晰的信息层级。
7. **匠心 (Craft)**：重视每一个细节。精细打磨字体排版、对齐度、动画过渡与代码类型安全，保持零 Lint 错误。
8. **愉悦感 (Delight)**：融入人文关怀。拒绝冰冷生硬的技术黑话与虚夸词，使用亲切诚恳的人本化语言。

---

## 2. HIG 无障碍设计铁律 (Accessibility Inviolable Rules)

详见 [`ACCESSIBILITY.md`](./ACCESSIBILITY.md)：
- **严禁仅凭颜色传达状态**：任何状态（达标、错误、警告）必须伴随具象文字或图标。
- **44 × 44 px 触控热区**：所有可交互元素保证最小点击区域为 44px，包括图标按钮与输入控件。
- **VoiceOver / 屏幕朗读友好**：所有图标按钮必须配置具体意图的 `aria-label`（如“返回食谱列表”），严禁出现无语义的空白按钮或冗余的“xx按钮”字样；纯装饰性 SVG 必须显式标记 `aria-hidden="true"`。
- **动效偏好适配**：必须适配 `prefers-reduced-motion: reduce`，避免强迫用户经历眩晕位移动画。
- **键盘导航与高亮轮廓**：全功能支持键盘访问，配置清晰的 `:focus-visible` 2px 轮廓。

---

## 3. 视觉与排版铁律 (Savee Minimalist & Anti-AI-Slop)

- **色彩规范**：
  - 画布背景：`#FAFAFA`
  - 卡片与容器：纯白 `#FFFFFF`
  - 边框：统一为 0.5px 极细浅灰描边 (`#E0E0E0`)，定义在 `.card-border` / `.border-hairline-*` 中
  - 文字体系：正黑与中性灰（`#0A0A0A`、`#525252`、`#A3A3A3`），严禁任何紫色、蓝色渐变或霓虹高亮
- **无阴影装饰**：严禁引入 `shadow-*` 或发光滤镜，完全通过纯白块面、物理极细边框与严整对齐建立视觉节奏。
- **结构扁平化**：杜绝嵌套卡片（卡片套卡片），数据指标使用纯细线（`divide-[#E0E0E0]`）横向平铺展示。

---

## 4. 语言与文案规范 (Human-Centered Copywriting)

- **严禁词汇**：禁止使用工程技术术语（如 Database、JSON、模式、数据表、后台等）及营销浮夸词（如 Precise、Instant、Supercharge、Empower 等）。
- **自然生活化**：使用“私房食谱”、“属于哪一餐”、“想吃点什么”、“为你保存好”等温和、具象、亲切的人本语言。
